<%= form_with(model: group_member, local: true) do |form| %>
  <% if group_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group_member.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% group_member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :group_id, id: :group_member_group_id %>
  </div>

  <div class="field">
    <%= form.label :friendship_id %>
    <%= form.text_field :friendship_id, id: :group_member_friendship_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
